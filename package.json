{
  "name": "ppt-fixer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --env-file-if-exists=.env.test --test test/*.test.js"
  },
  "dependencies": {
    "fflate": "0.8.3",
    "image-in-browser": "3.6.0"
  },
  "devDependencies": {
    "vite": "8.3.0"
  }
}
